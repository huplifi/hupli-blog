---
import Layout from '../../layouts/Layout.astro';
import { getCollection, render } from 'astro:content';

export async function getStaticPaths() {
  const posts = await getCollection('blog');
  return posts.map((post) => ({
    params: { slug: post.id.replace(/\.mdx?$/, '') },
    props: { slug: post.id.replace(/\.mdx?$/, '') },
  }));
}

const { slug } = Astro.props;
const posts = await getCollection('blog');
const post = posts.find(p => p.id.replace(/\.mdx?$/, '') === slug);
if (!post) return Astro.redirect('/404');
const { Content, headings } = await render(post);

function formatDate(date: Date) {
  return date.toLocaleDateString('fi-FI', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
}
---

<Layout title={post.data.title} description={post.data.description}>
  <article class="max-w-2xl mx-auto">
    <header class="mb-12 pb-8 blog-header border-b">
      <h1 class="font-serif text-4xl md:text-5xl font-bold mb-6 leading-tight">{post.data.title}</h1>
      <div class="flex flex-wrap items-center gap-4 text-sm font-mono">
        <time datetime={post.data.pubDate.toISOString()}>
          {formatDate(post.data.pubDate)}
        </time>
        {post.data.tags && post.data.tags.length > 0 && (
          <div class="flex gap-2 flex-wrap">
            {post.data.tags.map((tag) => (
              <span class="tag text-xs px-2 py-1 rounded">
                {tag}
              </span>
            ))}
          </div>
        )}
      </div>
    </header>

    <div class="prose prose-lg">
      <Content />
    </div>

    <div class="mt-16 pt-8 border-t" style="border-color: var(--color-border)">
      <a href="/" class="blog-back-link inline-flex items-center gap-2 text-sm font-mono">
        <span>←</span>
        <span>Takaisin blogiin</span>
      </a>
    </div>
  </article>
</Layout>